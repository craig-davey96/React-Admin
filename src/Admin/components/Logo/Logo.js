import React from 'react';
import PropTypes from 'prop-types';
import styles from './Logo.module.css';

const Logo = () => (
    <img src="/favicon.svg"/>
);

Logo.propTypes = {};

Logo.defaultProps = {};

export default Logo;
